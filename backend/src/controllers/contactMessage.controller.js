// ============================================================
// Contrôleur contactMessage — TODO : à implémenter
// ============================================================
// const model = require('../models/contactMessage.model');

// exports.getAll = async (req, res, next) => { };
// exports.create = async (req, res, next) => { };

module.exports = {};
