// ============================================================
// Contrôleur partner — TODO : à implémenter
// ============================================================
// const model = require('../models/partner.model');

// exports.getAll = async (req, res, next) => { };
// exports.create = async (req, res, next) => { };

module.exports = {};
