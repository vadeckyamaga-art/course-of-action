// ============================================================
// Service témoignages — TODO : à implémenter au fur et à mesure
// des routes backend disponibles
// ============================================================
import api from './api';

// export async function submitTestimonial(data) {
//   const { data: response } = await api.post('/testimonials', data);
//   return response;
// }

// export async function checkTestimonialStatus(trackingCode) {
//   const { data } = await api.get(`/testimonials/status/${trackingCode}`);
//   return data;
// }
